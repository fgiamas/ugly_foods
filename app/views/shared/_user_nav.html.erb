<div class="nav-bar-user d-flex justify-content-between" style="height: 100px; width: 100%; padding: 10px; padding-left: 20px; background-color: #ECF9FE;">
    <%= link_to root_path do %>
      <%= image_tag("logo4.png", :alt => "ugly goods", style: 'width: 40px; height: 40px;') %>
    <% end %>
    <div>
    <div class="d-flex justify-content-end">
      <% if user_signed_in? %>
        <div class="d-flex justify-content-end">
          <% if !current_user.shop %>
            <div>
              <%= render "shops/shopping_cart" %>
            </div>
          <% end %>
          <div>
             <div class="btn-group">

              <button type="button" class="btn dropdown-toggle dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="position:relative; color: #4C4B60;">
                <i class="fas fa-user fa-2x" style="color:#4C4B60; padding-top: 20px;"></i>
              </button>

              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                <%= link_to "Dashboard", dashboard_path, class: "dropdown-item" %>
                <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
              </div>
              </div>
          </div>
        </div>
      <% end %>
    </div>
    </div>

    <% if !user_signed_in? %>
      <div>
        <%= render 'shared/login_button' %>
      </div>
    <% end %>

</div>
